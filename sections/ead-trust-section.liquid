{%- comment -%}
  Euro Auto Design — Trust-Building Homepage Section
  Shopify 2.0 compatible | sections/ead-trust-section.liquid
{%- endcomment -%}

{%- liquid
  assign accent     = section.settings.accent_color
  assign bg         = section.settings.background_color
  assign card_bg    = section.settings.card_background_color
  assign border_col = section.settings.border_color
  assign title_col  = section.settings.title_color
  assign text_col   = section.settings.text_color
  assign pt         = section.settings.padding_top
  assign pb         = section.settings.padding_bottom
-%}

{%- style -%}
  #ead-trust-{{ section.id }} {
    background-color: {{ bg }};
    padding-top: {{ pt }}px;
    padding-bottom: {{ pb }}px;
  }
  #ead-trust-{{ section.id }} .ead-trust__eyebrow         { color: {{ accent }}; }
  #ead-trust-{{ section.id }} .ead-trust__eyebrow::before,
  #ead-trust-{{ section.id }} .ead-trust__eyebrow::after  { background: {{ accent }}; }
  #ead-trust-{{ section.id }} .ead-trust__heading         { color: {{ title_col }}; }
  #ead-trust-{{ section.id }} .ead-trust__subtitle        { color: {{ text_col }}; }
  #ead-trust-{{ section.id }} .ead-trust__divider         { background: {{ accent }}; }

  #ead-trust-{{ section.id }} .ead-trust__card {
    background-color: {{ card_bg }};
    border-color: {{ border_col }};
  }
  #ead-trust-{{ section.id }} .ead-trust__card-title      { color: {{ title_col }}; }
  #ead-trust-{{ section.id }} .ead-trust__card-text       { color: {{ text_col }}; }

  #ead-trust-{{ section.id }} .ead-trust__card-icon-wrap {
    color: {{ accent }};
    background: {{ accent }}12;
    border: 1px solid {{ accent }}20;
  }

  /* Hover states scoped to this instance */
  @media (hover: hover) {
    #ead-trust-{{ section.id }} .ead-trust__card:hover {
      border-color: {{ accent }}55;
      box-shadow:
        0 0 0 1px {{ accent }}22,
        0 16px 48px {{ accent }}14,
        0 4px 16px rgba(0,0,0,0.4);
    }
    #ead-trust-{{ section.id }} .ead-trust__card:hover .ead-trust__card-icon-wrap {
      background: {{ accent }}1e;
      border-color: {{ accent }}44;
    }
    #ead-trust-{{ section.id }} .ead-trust__card:hover .ead-trust__card-icon-wrap svg {
      filter: drop-shadow(0 0 7px {{ accent }}bb);
    }
    #ead-trust-{{ section.id }} .ead-trust__card:hover .ead-trust__card-accent-bar {
      opacity: 1;
      transform: scaleX(1);
    }
  }
  #ead-trust-{{ section.id }} .ead-trust__card-accent-bar { background: {{ accent }}; }
{%- endstyle -%}

<section
  id="ead-trust-{{ section.id }}"
  class="ead-trust"
  aria-label="{{ section.settings.title | escape }}"
>
  <div class="ead-trust__container">

    {%- if section.settings.eyebrow != blank or section.settings.title != blank or section.settings.subtitle != blank -%}
      <header class="ead-trust__header ead-animate" style="--delay:0ms">
        {%- if section.settings.eyebrow != blank -%}
          <p class="ead-trust__eyebrow">{{ section.settings.eyebrow }}</p>
        {%- endif -%}
        {%- if section.settings.title != blank -%}
          <h2 class="ead-trust__heading">{{ section.settings.title }}</h2>
        {%- endif -%}
        <span class="ead-trust__divider" aria-hidden="true"></span>
        {%- if section.settings.subtitle != blank -%}
          <p class="ead-trust__subtitle">{{ section.settings.subtitle }}</p>
        {%- endif -%}
      </header>
    {%- endif -%}

    <div class="ead-trust__grid">
      {%- for block in section.blocks -%}
        {%- assign card_delay  = forloop.index0 | times: 100 | plus: 120 -%}
        {%- assign float_delay = forloop.index0 | times: 700 -%}
        <article
          class="ead-trust__card ead-animate"
          style="--delay:{{ card_delay }}ms; --float-delay:{{ float_delay }}ms;"
          {{ block.shopify_attributes }}
        >
          {%- comment %} Inner gradient shimmer layer {%- endcomment %}
          <span class="ead-trust__card-shimmer" aria-hidden="true"></span>

          <div class="ead-trust__card-icon-wrap" aria-hidden="true">
            {%- case block.settings.icon -%}

              {%- when 'diamond' -%}
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false"><path d="M2.7 10.3a2.41 2.41 0 0 0 0 3.41l7.59 7.59a2.41 2.41 0 0 0 3.41 0l7.59-7.59a2.41 2.41 0 0 0 0-3.41L13.7 2.71a2.41 2.41 0 0 0-3.41 0Z"/><line x1="12" x2="12" y1="2.5" y2="21.5"/><path d="M2.5 10.5h19"/></svg>

              {%- when 'shield' -%}
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/><path d="m9 12 2 2 4-4"/></svg>

              {%- when 'gauge' -%}
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false"><path d="M12 2a10 10 0 1 0 10 10"/><path d="M12 6v2"/><path d="m16.24 7.76-1.42 1.42"/><path d="M18 12h2"/><path d="M12 18v2"/><path d="m7.76 16.24 1.42-1.42"/><path d="M6 12H4"/><path d="m11.5 12.5 3.34-3.34"/><circle cx="12" cy="12" r="2"/></svg>

              {%- when 'headset' -%}
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false"><path d="M3 18v-6a9 9 0 0 1 18 0v6"/><path d="M21 19a2 2 0 0 1-2 2h-1a2 2 0 0 1-2-2v-3a2 2 0 0 1 2-2h3z"/><path d="M3 19a2 2 0 0 0 2 2h1a2 2 0 0 0 2-2v-3a2 2 0 0 0-2-2H3z"/></svg>

              {%- when 'star' -%}
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>

              {%- when 'award' -%}
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false"><circle cx="12" cy="8" r="6"/><path d="M15.477 12.89 17 22l-5-3-5 3 1.523-9.11"/></svg>

              {%- when 'wrench' -%}
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false"><path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/></svg>

              {%- when 'check-circle' -%}
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>

              {%- when 'users' -%}
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>

              {%- when 'zap' -%}
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>

              {%- when 'flag' -%}
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false"><path d="M4 15s1-1 4-1 5 2 8 2 4-1 4-1V3s-1 1-4 1-5-2-8-2-4 1-4 1z"/><line x1="4" x2="4" y1="22" y2="15"/></svg>

              {%- else -%}
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>

            {%- endcase -%}
          </div>

          {%- if block.settings.heading != blank -%}
            <h3 class="ead-trust__card-title">{{ block.settings.heading }}</h3>
          {%- endif -%}

          {%- if block.settings.description != blank -%}
            <p class="ead-trust__card-text">{{ block.settings.description }}</p>
          {%- endif -%}

          <span class="ead-trust__card-accent-bar" aria-hidden="true"></span>
        </article>
      {%- endfor -%}
    </div>

  </div>
</section>

<style>
  /* ── Reset ──────────────────────────────────────────────── */
  .ead-trust,
  .ead-trust *,
  .ead-trust *::before,
  .ead-trust *::after { box-sizing: border-box; }

  /* ── Section ────────────────────────────────────────────── */
  .ead-trust {
    position: relative;
    width: 100%;
    overflow: hidden;
  }

  .ead-trust__container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 28px;
  }

  /* ── Header ─────────────────────────────────────────────── */
  .ead-trust__header {
    text-align: center;
    margin-bottom: 48px;
  }

  .ead-trust__eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.24em;
    text-transform: uppercase;
    margin: 0 0 14px;
    font-family: inherit;
  }

  .ead-trust__eyebrow::before,
  .ead-trust__eyebrow::after {
    content: '';
    display: block;
    width: 26px;
    height: 1px;
    opacity: 0.5;
    flex-shrink: 0;
  }

  .ead-trust__heading {
    font-size: clamp(20px, 2.5vw, 28px);
    font-weight: 800;
    letter-spacing: -0.025em;
    line-height: 1.2;
    margin: 0 0 14px;
    font-family: inherit;
  }

  .ead-trust__divider {
    display: block;
    width: 36px;
    height: 2px;
    margin: 0 auto 14px;
    border-radius: 2px;
    opacity: 0.65;
  }

  .ead-trust__subtitle {
    font-size: 14px;
    line-height: 1.65;
    opacity: 0.58;
    max-width: 480px;
    margin: 0 auto;
    font-family: inherit;
  }

  /* ── Grid — desktop: 4 equal columns ───────────────────── */
  .ead-trust__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
    align-items: stretch;
  }

  /* ── Card ───────────────────────────────────────────────── */
  .ead-trust__card {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;      /* center icon + text horizontally */
    text-align: center;
    padding: 32px 22px 36px;
    min-height: 260px;        /* near-square at typical 4-col widths  */
    border-radius: 16px;
    border: 1px solid transparent;
    overflow: hidden;
    cursor: default;
    transition:
      transform 0.35s cubic-bezier(0.23, 1, 0.32, 1),
      box-shadow 0.35s cubic-bezier(0.23, 1, 0.32, 1),
      border-color 0.35s ease;
  }

  /* Top-left highlight shimmer for glass-like depth */
  .ead-trust__card-shimmer {
    position: absolute;
    inset: 0;
    border-radius: inherit;
    background: linear-gradient(
      135deg,
      rgba(255,255,255,0.04) 0%,
      rgba(255,255,255,0.01) 40%,
      transparent 70%
    );
    pointer-events: none;
  }

  @media (hover: hover) {
    .ead-trust__card:hover {
      transform: translateY(-6px);
    }
  }

  /* Gold accent bar — slides in from left on hover */
  .ead-trust__card-accent-bar {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 2px;
    border-radius: 0 0 16px 16px;
    opacity: 0;
    transform: scaleX(0.35);
    transform-origin: left center;
    transition:
      opacity  0.35s ease,
      transform 0.4s cubic-bezier(0.23, 1, 0.32, 1);
  }

  /* ── Icon Wrapper ───────────────────────────────────────── */
  .ead-trust__card-icon-wrap {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 56px;
    height: 56px;
    border-radius: 14px;
    margin-bottom: 20px;
    flex-shrink: 0;
    transition:
      background 0.35s ease,
      border-color 0.35s ease;
  }

  .ead-trust__card-icon-wrap svg {
    width: 26px;
    height: 26px;
    display: block;
    flex-shrink: 0;
    /* Floating animation — staggered per card via --float-delay */
    animation: ead-float 3.8s ease-in-out var(--float-delay, 0ms) infinite;
    transition: filter 0.35s ease;
    will-change: transform;
  }

  /* ── Icon Float Keyframes ───────────────────────────────── */
  @keyframes ead-float {
    0%   { transform: translateY(0)    rotateZ(0deg); }
    25%  { transform: translateY(-4px) rotateZ(1.2deg); }
    50%  { transform: translateY(-6px) rotateZ(0deg); }
    75%  { transform: translateY(-3px) rotateZ(-1.2deg); }
    100% { transform: translateY(0)    rotateZ(0deg); }
  }

  /* ── Card Text ──────────────────────────────────────────── */
  .ead-trust__card-title {
    font-size: 14px;
    font-weight: 700;
    letter-spacing: 0.01em;
    line-height: 1.3;
    margin: 0 0 10px;
    font-family: inherit;
  }

  .ead-trust__card-text {
    font-size: 12.5px;
    line-height: 1.7;
    opacity: 0.66;
    margin: 0;
    font-family: inherit;
    flex: 1;
  }

  /* ── Fade-Up Entrance Animation ─────────────────────────── */
  .ead-animate {
    opacity: 0;
    transform: translateY(24px);
    transition:
      opacity   0.65s cubic-bezier(0.23, 1, 0.32, 1),
      transform 0.65s cubic-bezier(0.23, 1, 0.32, 1);
    transition-delay: var(--delay, 0ms);
    will-change: opacity, transform;
  }

  .ead-animate.ead-visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* ── Tablet: 2×2 grid ───────────────────────────────────── */
  @media screen and (max-width: 1024px) {
    .ead-trust__grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 14px;
    }
    .ead-trust__card {
      min-height: 220px;
    }
  }

  /* ── Mobile: single column, compact stacked cards ──────── */
  @media screen and (max-width: 640px) {
    .ead-trust__container {
      padding: 0 16px;
    }
    .ead-trust__header {
      margin-bottom: 32px;
    }
    .ead-trust__grid {
      grid-template-columns: 1fr;
      gap: 10px;
    }
    .ead-trust__card {
      /* Horizontal layout on mobile: icon left, text right */
      flex-direction: row;
      align-items: flex-start;
      text-align: left;
      padding: 20px 18px;
      min-height: 0;
      gap: 16px;
      border-radius: 12px;
    }
    .ead-trust__card-icon-wrap {
      width: 44px;
      height: 44px;
      border-radius: 10px;
      margin-bottom: 0;
      flex-shrink: 0;
    }
    .ead-trust__card-icon-wrap svg {
      width: 22px;
      height: 22px;
    }
    .ead-trust__card-body {
      display: flex;
      flex-direction: column;
    }
    .ead-trust__card-title {
      font-size: 13.5px;
      margin-bottom: 6px;
    }
    .ead-trust__card-text {
      font-size: 12px;
    }
    /* Accent bar on left edge for mobile */
    .ead-trust__card-accent-bar {
      top: 0;
      bottom: 0;
      right: auto;
      left: 0;
      width: 2px;
      height: auto;
      border-radius: 12px 0 0 12px;
      transform: scaleY(0.35);
      transform-origin: top center;
    }
    @media (hover: hover) {
      .ead-trust__card:hover .ead-trust__card-accent-bar {
        transform: scaleY(1);
      }
    }
  }

  /* ── Reduced Motion ─────────────────────────────────────── */
  @media (prefers-reduced-motion: reduce) {
    .ead-animate {
      opacity: 1;
      transform: none;
      transition: none;
    }
    .ead-trust__card,
    .ead-trust__card-accent-bar { transition: none; }
    .ead-trust__card-icon-wrap svg { animation: none; }
  }
</style>

<script>
  (function () {
    'use strict';

    function runTrustSection(id) {
      var section = document.getElementById('ead-trust-' + id);
      if (!section) return;

      var els = section.querySelectorAll('.ead-animate');
      if (!els.length) return;

      if (!('IntersectionObserver' in window)) {
        Array.prototype.forEach.call(els, function (el) {
          el.classList.add('ead-visible');
        });
        return;
      }

      var io = new IntersectionObserver(
        function (entries) {
          Array.prototype.forEach.call(entries, function (entry) {
            if (entry.isIntersecting) {
              entry.target.classList.add('ead-visible');
              io.unobserve(entry.target);
            }
          });
        },
        { threshold: 0.1, rootMargin: '0px 0px -28px 0px' }
      );

      Array.prototype.forEach.call(els, function (el) { io.observe(el); });
    }

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', function () {
        runTrustSection('{{ section.id }}');
      });
    } else {
      runTrustSection('{{ section.id }}');
    }

    document.addEventListener('shopify:section:load', function (e) {
      if (e && e.detail && e.detail.sectionId === '{{ section.id }}') {
        runTrustSection('{{ section.id }}');
      }
    });
  }());
</script>

{% schema %}
{
  "name": "EAD — Trust Section",
  "tag": "section",
  "class": "ead-trust-section-wrapper",
  "settings": [
    {
      "type": "header",
      "content": "Section Header"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow Label",
      "default": "Our Promise"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Section Title",
      "default": "Why Enthusiasts Choose Euro Auto Design"
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "label": "Subtitle (optional)"
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Section Background",
      "default": "#0d0d0d"
    },
    {
      "type": "color",
      "id": "card_background_color",
      "label": "Card Background",
      "default": "#181818"
    },
    {
      "type": "color",
      "id": "border_color",
      "label": "Card Border",
      "default": "#282828"
    },
    {
      "type": "color",
      "id": "accent_color",
      "label": "Accent Color (gold)",
      "default": "#c9a84c"
    },
    {
      "type": "color",
      "id": "title_color",
      "label": "Heading & Title Color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Body Text Color",
      "default": "#d8d8d8"
    },
    {
      "type": "header",
      "content": "Spacing"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Top Padding",
      "min": 24,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 72
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Bottom Padding",
      "min": 24,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 72
    }
  ],
  "blocks": [
    {
      "type": "trust_card",
      "name": "Trust Card",
      "limit": 8,
      "settings": [
        {
          "type": "select",
          "id": "icon",
          "label": "Icon",
          "options": [
            { "value": "diamond",      "label": "Diamond (Premium)" },
            { "value": "shield",       "label": "Shield (Trust)" },
            { "value": "gauge",        "label": "Gauge (Performance)" },
            { "value": "headset",      "label": "Headset (Support)" },
            { "value": "star",         "label": "Star (Excellence)" },
            { "value": "award",        "label": "Award (Quality)" },
            { "value": "wrench",       "label": "Wrench (Technical)" },
            { "value": "check-circle", "label": "Check Circle (Fitment)" },
            { "value": "users",        "label": "Users (Team)" },
            { "value": "zap",          "label": "Zap (Performance)" },
            { "value": "flag",         "label": "Flag (Enthusiast)" }
          ],
          "default": "diamond"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Card Heading",
          "default": "Trust Point Title"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "Card Description",
          "default": "A short description that builds customer confidence and communicates your brand value clearly."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "EAD — Trust Section",
      "blocks": [
        {
          "type": "trust_card",
          "settings": {
            "icon": "diamond",
            "heading": "Premium European Upgrades",
            "description": "Curated carbon fiber aero, lighting, and styling upgrades designed for BMW, Audi, Mercedes-Benz, Porsche, and other performance vehicles."
          }
        },
        {
          "type": "trust_card",
          "settings": {
            "icon": "check-circle",
            "heading": "Fitment-Focused Support",
            "description": "Our team helps customers confirm compatibility and make more confident upgrade decisions before and after purchase."
          }
        },
        {
          "type": "trust_card",
          "settings": {
            "icon": "headset",
            "heading": "Real Human Assistance",
            "description": "From order updates to product questions, our support team provides clear communication throughout the customer experience."
          }
        },
        {
          "type": "trust_card",
          "settings": {
            "icon": "zap",
            "heading": "Built for Enthusiasts",
            "description": "Every product is selected with aggressive styling, premium finish, and enthusiast-level expectations in mind."
          }
        }
      ]
    }
  ]
}
{% endschema %}
